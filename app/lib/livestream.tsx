'use server';
/**
 * Video has a unique ID that can be put into a crafted URL to get the
 * latest broadcast.
 */
// TODO: Poll for latest broadcast one a week
export async function fetchBroadcast(): Promise<string> {
    var latestBroadcast = '';
    
    return latestBroadcast;
}